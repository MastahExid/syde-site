## Packages
framer-motion | Complex scroll animations and transitions
lucide-react | Icons for the UI (already in base, but emphasizing usage)
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
- Dark mode is forced via CSS variables in index.css
- "Just SYDE It" typography requires careful font-style mixing
- Images for team members will use placeholders if API returns empty strings, but API integration is fully implemented
- Horizontal scroll uses framer-motion for smooth entrance animations
